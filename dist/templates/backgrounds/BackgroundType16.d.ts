export interface ISvgProps {
    mainColor?: string;
    subColor?: string;
    extraColor1?: string;
}
export declare function createBackgroundType16({ mainColor, subColor, extraColor1, }: ISvgProps): string;
