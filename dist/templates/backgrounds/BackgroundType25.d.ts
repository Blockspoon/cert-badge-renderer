export interface ISvgProps {
    mainColor?: string;
    subColor?: string;
    extraColor?: string;
}
export declare function createBackgroundType25({ mainColor, subColor, extraColor, }: ISvgProps): string;
